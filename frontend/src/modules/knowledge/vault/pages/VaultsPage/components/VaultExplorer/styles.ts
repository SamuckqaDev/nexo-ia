import styled from "styled-components";

export const Library = styled.div`display:grid;height:100%;min-height:0;grid-template-rows:auto auto minmax(0,1fr);gap:${({ theme }) => theme.spacing.sm};padding:${({ theme }) => theme.spacing.md};overflow:hidden;`;
export const VaultList = styled.div`display:grid;min-height:0;align-content:start;gap:.35rem;overflow:auto;overscroll-behavior:contain;`;
export const VaultButton = styled.button<{ $active: boolean; $team: boolean }>`position:relative;display:grid;grid-template-columns:auto minmax(0,1fr) auto;align-items:center;gap:${({ theme }) => theme.spacing.sm};width:100%;border:1px solid ${({ theme, $active }) => ($active ? theme.colors.lineStrong : "transparent")};border-radius:${({ theme }) => theme.radius.control};padding:.75rem .75rem .75rem .95rem;overflow:hidden;background:${({ theme, $active, $team }) => (!$active ? "transparent" : $team ? `linear-gradient(100deg, color-mix(in srgb, ${theme.colors.accent} 16%, transparent), ${theme.colors.surfaceAccent})` : theme.colors.surfaceAccent)};color:${({ theme, $active, $team }) => ($active ? ($team ? theme.colors.accentSoft : theme.colors.primary) : theme.colors.textMuted)};font:inherit;text-align:left;cursor:pointer;transition:background .15s ease,color .15s ease;&::before{content:"";position:absolute;top:.5rem;bottom:.5rem;left:0;width:.2rem;border-radius:${({ theme }) => theme.radius.round};background:${({ theme, $team }) => ($team ? theme.colors.accent : theme.colors.primary)};opacity:${({ $active }) => ($active ? 1 : 0.45)};}&:hover{background:${({ theme, $team }) => ($team ? `linear-gradient(100deg, color-mix(in srgb, ${theme.colors.accent} 14%, transparent), ${theme.colors.surfaceAccent})` : theme.colors.surfaceAccent)};}`;
export const VaultCopy = styled.span<{ $team: boolean }>`min-width:0;strong,span{display:block;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}strong{color:${({ theme }) => theme.colors.text};font-size:.72rem;}span{margin-top:.18rem;color:${({ theme, $team }) => ($team ? theme.colors.accentSoft : theme.colors.textSubtle)};font-size:.58rem;text-transform:capitalize;}`;
