import styled from "styled-components";
export const Container = styled.div`position:relative;`;
export const Trigger = styled.button`display:flex;align-items:center;gap:${({theme})=>theme.spacing.sm};height:2.7rem;border:1px solid ${({theme})=>theme.colors.line};border-radius:${({theme})=>theme.radius.control};padding:.3rem .65rem .3rem .3rem;background:${({theme})=>theme.colors.surfaceStrong};color:${({theme})=>theme.colors.text};font:inherit;cursor:pointer;&:hover{border-color:${({theme})=>theme.colors.lineStrong};}@media(max-width:32rem){padding-right:.3rem;&>span+span,&>svg{display:none;}}`;
export const Avatar = styled.span`position:relative;display:grid;width:2rem;height:2rem;place-items:center;border-radius:${({theme})=>theme.radius.control};overflow:hidden;background:${({theme})=>theme.colors.backgroundElevated};color:${({theme})=>theme.colors.primarySoft};font-size:.7rem;font-weight:700;&::after{position:absolute;z-index:2;inset:0;border:1px solid ${({theme})=>theme.colors.line};border-radius:${({theme})=>theme.radius.control};content:"";pointer-events:none;}`;
export const AvatarImage = styled.img`position:absolute;width:100%;height:100%;border-radius:${({theme})=>theme.radius.control};object-fit:cover;`;
export const Menu = styled.div`position:absolute;z-index:20;top:calc(100% + .65rem);right:0;width:min(19rem,calc(100vw - 2rem));padding:${({theme})=>theme.spacing.sm};border:1px solid ${({theme})=>theme.colors.line};border-radius:${({theme})=>theme.radius.control};background:${({theme})=>theme.colors.surfaceStrong};box-shadow:${({theme})=>theme.shadow};`;
export const Profile = styled.div`padding:${({theme})=>theme.spacing.sm};border-bottom:1px solid ${({theme})=>theme.colors.line};`;
export const Name = styled.strong`display:block;font-size:.88rem;`;
export const Email = styled.span`display:block;overflow:hidden;color:${({theme})=>theme.colors.textMuted};font-size:.72rem;text-overflow:ellipsis;white-space:nowrap;`;
export const MenuButton = styled.button`display:flex;align-items:center;gap:${({theme})=>theme.spacing.sm};width:100%;margin-top:.25rem;border:1px solid transparent;border-radius:${({theme})=>theme.radius.control};padding:.7rem;background:transparent;color:${({theme})=>theme.colors.textMuted};font:inherit;font-size:.8rem;text-align:left;cursor:pointer;&:hover{border-color:${({theme})=>theme.colors.line};background:${({theme})=>theme.colors.surfaceAccent};color:${({theme})=>theme.colors.primarySoft};}`;
